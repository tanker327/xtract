{
    "rules": [
        {
            "name": "git-operations",
            "description": "Before executing any git operations (add, commit, push, pull, etc.), ALWAYS get explicit confirmation from the user. Do not execute any git commands without prior user confirmation, even if the user's request seems to imply git operations.",
            "enabled": true
        }
    ],
    "python": {
        "formatting": {
            "provider": "black"
        },
        "analysis": {
            "enableTypeChecking": true,
            "extraPaths": ["src"]
        }
    },
    "terminal": {
        "requireUserApprovalForAllCommands": true
    },
    "editor": {
        "formatOnSave": true,
        "tabSize": 4,
        "insertSpaces": true
    }
}
